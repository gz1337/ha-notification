# =============================================================================
# Notify Manager - Benutzerfreundliche Services
# Optimiert für einfache Bedienung in Automationen & Szenen
# =============================================================================

# -----------------------------------------------------------------------------
# HAUPT-SERVICE: Einfache Benachrichtigung
# -----------------------------------------------------------------------------
send_notification:
  name: 📱 Benachrichtigung senden
  description: Sendet eine Benachrichtigung an dein Smartphone.
  fields:
    title:
      name: Titel
      description: Titel der Benachrichtigung
      required: true
      default: "Home Assistant"
      example: "Willkommen"
      selector:
        text:
    message:
      name: Nachricht
      description: Was möchtest du mitteilen?
      required: true
      example: "Willkommen zu Hause!"
      selector:
        text:
          multiline: true
    priority:
      name: Priorität
      description: Wie wichtig ist diese Nachricht?
      required: false
      default: "normal"
      selector:
        select:
          mode: dropdown
          options:
            - label: "🔇 Leise"
              value: "low"
            - label: "📱 Normal"
              value: "normal"
            - label: "🔔 Wichtig"
              value: "high"
            - label: "🚨 Kritisch (durchbricht DND)"
              value: "critical"
    tag:
      name: Tag (optional)
      description: Gleicher Tag = ersetzt vorherige Nachricht
      required: false
      advanced: true
      selector:
        text:
    clickAction:
      name: Bei Klick öffnen (optional)
      description: Dashboard-Pfad oder URL
      required: false
      advanced: true
      example: "/lovelace/cameras"
      selector:
        text:

# -----------------------------------------------------------------------------
# MIT BUTTONS: Interaktive Benachrichtigung
# -----------------------------------------------------------------------------
send_actionable:
  name: 🔘 Benachrichtigung mit Buttons
  description: Benachrichtigung mit Aktions-Buttons zum Antippen.
  fields:
    title:
      name: Titel
      required: true
      example: "Türklingel"
      selector:
        text:
    message:
      name: Nachricht
      required: true
      example: "Jemand ist an der Tür"
      selector:
        text:
          multiline: true
    actions:
      name: Buttons
      description: |
        Button-Liste. Beispiel:
        - action: "OPEN"
          title: "🔓 Öffnen"
        - action: "IGNORE"  
          title: "Ignorieren"
      required: true
      selector:
        object:
    priority:
      name: Priorität
      required: false
      default: "high"
      selector:
        select:
          mode: dropdown
          options:
            - label: "📱 Normal"
              value: "normal"
            - label: "🔔 Wichtig"
              value: "high"
            - label: "🚨 Kritisch"
              value: "critical"
    tag:
      name: Tag
      required: false
      advanced: true
      selector:
        text:
    persistent:
      name: Nicht automatisch verschwinden
      required: false
      default: false
      advanced: true
      selector:
        boolean:

# -----------------------------------------------------------------------------
# MIT KAMERA/BILD
# -----------------------------------------------------------------------------
send_with_image:
  name: 📷 Benachrichtigung mit Bild
  description: Zeigt ein Kamerabild oder Foto an.
  fields:
    title:
      name: Titel
      required: true
      example: "Bewegung erkannt"
      selector:
        text:
    message:
      name: Nachricht
      required: true
      example: "Bewegung an der Haustür"
      selector:
        text:
    camera_entity:
      name: Kamera
      description: Wähle eine Kamera für den Snapshot
      required: false
      selector:
        entity:
          filter:
            domain: camera
    image:
      name: Oder Bild-URL
      description: Alternativ eine URL zu einem Bild
      required: false
      example: "/local/snapshot.jpg"
      selector:
        text:
    actions:
      name: Buttons (optional)
      required: false
      advanced: true
      selector:
        object:
    priority:
      name: Priorität
      required: false
      default: "high"
      selector:
        select:
          mode: dropdown
          options:
            - label: "📱 Normal"
              value: "normal"
            - label: "🔔 Wichtig"
              value: "high"
            - label: "🚨 Kritisch"
              value: "critical"
    tag:
      name: Tag
      required: false
      advanced: true
      selector:
        text:

# -----------------------------------------------------------------------------
# SCHNELL-ALARM
# -----------------------------------------------------------------------------
send_alarm_confirmation:
  name: 🚨 Alarm-Bestätigung
  description: Schnelle Alarm-Benachrichtigung mit vorgefertigten Buttons.
  fields:
    title:
      name: Titel
      required: true
      default: "🚨 Alarm!"
      selector:
        text:
    message:
      name: Nachricht
      required: true
      example: "Bewegung erkannt während Abwesenheit"
      selector:
        text:
    template:
      name: Button-Vorlage
      description: Vorgefertigte Button-Kombinationen
      required: false
      default: "confirm_dismiss"
      selector:
        select:
          mode: dropdown
          options:
            - label: "✓ Bestätigen / ✗ Abbrechen"
              value: "confirm_dismiss"
            - label: "🚨 Alarm / ⏰ Später / ✓ OK"
              value: "alarm_response"
            - label: "🔓 Öffnen / 🔒 Sperren / 🚫 Ignorieren"
              value: "door_response"
            - label: "👍 Ja / 👎 Nein"
              value: "yes_no"
    tag:
      name: Tag
      required: false
      default: "alarm"
      advanced: true
      selector:
        text:

# -----------------------------------------------------------------------------
# TEXT-EINGABE
# -----------------------------------------------------------------------------
send_text_input:
  name: ✏️ Benachrichtigung mit Texteingabe
  description: Der Empfänger kann eine Textantwort senden.
  fields:
    title:
      name: Titel
      required: true
      example: "Einkaufsliste"
      selector:
        text:
    message:
      name: Nachricht
      required: true
      example: "Was soll ich mitbringen?"
      selector:
        text:
    input_title:
      name: Button-Text
      required: false
      default: "Antworten"
      selector:
        text:
    placeholder:
      name: Platzhalter-Text
      required: false
      default: "Nachricht eingeben..."
      selector:
        text:
    tag:
      name: Tag
      required: false
      advanced: true
      selector:
        text:

# -----------------------------------------------------------------------------
# LÖSCHEN
# -----------------------------------------------------------------------------
clear_notifications:
  name: 🗑️ Benachrichtigungen löschen
  description: Entfernt Benachrichtigungen vom Gerät.
  fields:
    tag:
      name: Tag
      description: Nur Nachrichten mit diesem Tag löschen (leer = alle)
      required: false
      selector:
        text:

# -----------------------------------------------------------------------------
# TEXT-TO-SPEECH (Android)
# -----------------------------------------------------------------------------
send_tts:
  name: 🔊 Text vorlesen (Android)
  description: Spricht Text auf dem Android-Gerät aus.
  fields:
    tts_text:
      name: Text
      description: Was soll vorgelesen werden?
      required: true
      example: "Achtung, die Waschmaschine ist fertig!"
      selector:
        text:
          multiline: true
    media_stream:
      name: Lautstärke
      required: false
      default: "music_stream"
      selector:
        select:
          mode: dropdown
          options:
            - label: "🔈 Normal"
              value: "music_stream"
            - label: "🔔 Alarm-Lautstärke"
              value: "alarm_stream"
            - label: "📢 Maximum (laut!)"
              value: "alarm_stream_max"

# -----------------------------------------------------------------------------
# KARTE MIT PIN (iOS)
# -----------------------------------------------------------------------------
send_map:
  name: 🗺️ Karte senden (iOS)
  description: Zeigt eine Karte mit Standort-Pin.
  fields:
    title:
      name: Titel
      required: false
      example: "Standort"
      selector:
        text:
    message:
      name: Nachricht
      required: true
      example: "Hier ist der Treffpunkt"
      selector:
        text:
    latitude:
      name: Breitengrad
      required: true
      example: "52.520008"
      selector:
        text:
    longitude:
      name: Längengrad
      required: true
      example: "13.404954"
      selector:
        text:
    second_latitude:
      name: Zweiter Pin - Breitengrad
      required: false
      advanced: true
      selector:
        text:
    second_longitude:
      name: Zweiter Pin - Längengrad
      required: false
      advanced: true
      selector:
        text:
    shows_line_between_points:
      name: Linie zwischen Pins
      required: false
      default: false
      advanced: true
      selector:
        boolean:

# -----------------------------------------------------------------------------
# VIDEO/AUDIO ANHANG
# -----------------------------------------------------------------------------
send_video:
  name: 🎬 Video/Audio senden
  description: Benachrichtigung mit Video- oder Audio-Anhang.
  fields:
    title:
      name: Titel
      required: true
      selector:
        text:
    message:
      name: Nachricht
      required: true
      selector:
        text:
    video:
      name: Video-URL
      description: Pfad zum Video
      required: false
      example: "/media/local/clip.mp4"
      selector:
        text:
    audio:
      name: Audio-URL
      description: Pfad zur Audio-Datei
      required: false
      example: "/media/local/sound.mp3"
      selector:
        text:
    tag:
      name: Tag
      required: false
      advanced: true
      selector:
        text:

# -----------------------------------------------------------------------------
# GERÄTE-BEFEHLE (Android)
# -----------------------------------------------------------------------------
device_command:
  name: 📲 Geräte-Befehl (Android)
  description: Steuert Android-Gerätefunktionen.
  fields:
    command:
      name: Befehl
      required: true
      selector:
        select:
          mode: dropdown
          options:
            - label: "🔇 Nicht stören AN/AUS"
              value: "command_dnd"
            - label: "🔔 Klingelton ändern"
              value: "command_ringer_mode"
            - label: "🔊 Lautstärke setzen"
              value: "command_volume_level"
            - label: "📱 Bildschirm einschalten"
              value: "command_screen_on"
            - label: "🔦 Taschenlampe"
              value: "command_flashlight"
            - label: "🌐 Dashboard öffnen"
              value: "command_webview"
            - label: "📦 App starten"
              value: "command_launch_app"
            - label: "⏯️ Medien-Steuerung"
              value: "command_media"
            - label: "📍 GPS High-Accuracy"
              value: "command_high_accuracy_mode"
            - label: "🔄 Sensoren aktualisieren"
              value: "command_update_sensors"
    data:
      name: Parameter
      description: |
        Zusätzliche Optionen je nach Befehl:
        DND: {"command": "priority_only"}
        Lautstärke: {"command": 50, "media_stream": "music_stream"}
        WebView: {"command": "/lovelace/home"}
        App: {"package_name": "com.spotify.music"}
      required: false
      selector:
        object:

# -----------------------------------------------------------------------------
# STANDORT ANFORDERN
# -----------------------------------------------------------------------------
request_location_update:
  name: 📍 Standort anfordern
  description: Fordert aktuellen Standort vom Gerät an.
  fields:
    target:
      name: Gerät
      description: Von welchem Gerät? (leer = alle)
      required: false
      selector:
        text:
          multiple: true

# -----------------------------------------------------------------------------
# FORTSCHRITTSANZEIGE (Android)
# -----------------------------------------------------------------------------
send_progress:
  name: 📊 Fortschritts-Benachrichtigung
  description: Zeigt einen Fortschrittsbalken an (Android).
  fields:
    title:
      name: Titel
      required: true
      example: "Download"
      selector:
        text:
    message:
      name: Nachricht
      required: true
      example: "Datei wird heruntergeladen..."
      selector:
        text:
    progress:
      name: Fortschritt
      description: Aktueller Wert (0-100)
      required: true
      selector:
        number:
          min: 0
          max: 100
          step: 1
          unit_of_measurement: "%"
          mode: slider
    tag:
      name: Tag
      description: Wichtig für Updates!
      required: true
      default: "progress"
      selector:
        text:

# -----------------------------------------------------------------------------
# TIMER/CHRONOMETER (Android)
# -----------------------------------------------------------------------------
send_chronometer:
  name: ⏱️ Timer-Benachrichtigung
  description: Benachrichtigung mit Countdown oder Stoppuhr (Android).
  fields:
    title:
      name: Titel
      required: true
      example: "Timer"
      selector:
        text:
    message:
      name: Nachricht
      required: true
      example: "Läuft..."
      selector:
        text:
    when:
      name: Sekunden
      description: Countdown-Zeit in Sekunden
      required: true
      selector:
        number:
          min: 1
          max: 86400
          step: 1
          unit_of_measurement: "s"
    tag:
      name: Tag
      required: true
      default: "timer"
      selector:
        text:
