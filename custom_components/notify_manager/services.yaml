# =============================================================================
# Notify Manager - Vereinfachte Services
# Alle Einstellungen werden im Frontend (Notify Manager Panel) vorgenommen
# =============================================================================

send_from_template:
  name: 📋 Benachrichtigung aus Vorlage senden
  description: >
    Sendet eine Benachrichtigung basierend auf einer im Notify Manager Panel erstellten Vorlage.

    👉 Vorlagen erstellen: Einstellungen → Geräte → Notify Manager → Panel öffnen → Tab "Vorlagen"

    Alle Optionen (Empfänger, Titel, Nachricht, Buttons, Bild, etc.) werden in der Vorlage definiert.
  fields:
    template_name:
      name: Vorlage auswählen
      description: "Name der Vorlage aus dem Notify Manager Panel. Erstelle Vorlagen unter: Panel → Vorlagen"
      required: true
      example: "🚪 Türklingel"
      selector:
        text:

# =============================================================================
# INTERNE SERVICES (werden automatisch vom Frontend aufgerufen)
# =============================================================================

save_templates:
  name: "[Intern] Vorlagen speichern"
  description: "Wird automatisch vom Frontend aufgerufen. Nicht manuell verwenden."
  fields:
    templates:
      name: Vorlagen-Daten
      required: true
      selector:
        object:

save_groups:
  name: "[Intern] Gruppen speichern"
  description: "Wird automatisch vom Frontend aufgerufen. Nicht manuell verwenden."
  fields:
    groups:
      name: Gruppen-Daten
      required: true
      selector:
        object:
