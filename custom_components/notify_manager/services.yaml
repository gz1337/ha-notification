# =============================================================================
# Notify Manager - Benachrichtigung aus Vorlage senden
# =============================================================================

send_from_template:
  name: "📋 Benachrichtigung aus Vorlage senden"
  description: >
    Wähle die Notify Manager Entity - ihre aktuelle Option wird als Vorlage verwendet.

    Die Entity zeigt alle deine Vorlagen (123Tester, etc.) als Optionen!
  fields:
    entity_id:
      name: "Vorlage wählen"
      description: "Klicke auf die Entity → wähle deine Vorlage aus dem Dropdown"
      required: true
      selector:
        entity:
          integration: notify_manager
          domain: select
