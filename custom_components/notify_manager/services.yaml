# =============================================================================
# Notify Manager - Nur EIN Service für Automationen
# Alle Einstellungen werden im Frontend (Notify Manager Panel) vorgenommen
# =============================================================================

send_from_template:
  name: "📋 Benachrichtigung aus Vorlage senden"
  description: >
    Sendet eine Benachrichtigung basierend auf einer im Notify Manager Panel erstellten Vorlage.

    📱 ALLE Optionen werden in der Vorlage konfiguriert:
    • Empfänger (Geräte oder Gruppen)
    • Titel und Nachricht
    • Buttons mit Action IDs
    • Bilder und Kamera-Snapshots
    • Android: Kanal, LED, Vibration, etc.
    • iOS: Sound, Badge, Critical Alerts, etc.

    👉 Vorlagen erstellen:
    Einstellungen → Geräte & Dienste → Notify Manager → Panel öffnen → Tab "Vorlagen"

    💡 Verfügbare Vorlagen anzeigen:
    Entität "select.notify_manager_active_notification" zeigt alle Vorlagennamen
  fields:
    template_name:
      name: "Vorlage"
      description: >
        Name der Vorlage aus dem Notify Manager Panel.
        Öffne das Panel um verfügbare Vorlagen zu sehen.
      required: true
      example: "🚪 Türklingel"
      selector:
        select:
          custom_value: true
          options:
            - "Vorlage im Panel erstellen..."
